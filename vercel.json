{
    "builds": [
      { "src": "index.py"}
    ],
    "routes": [
      { "src": "/(.*)", "dest": "index.py" }
    ],
    "buildCommand": "source irrigreat/bin/activate && pip install -r requirements.txt"
}
  